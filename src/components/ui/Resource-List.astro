---
import Card from './Card.astro';
import ResourceRow from './Resource-Row.astro';
import type { ResourceListItem } from './Resource-Row.astro';

interface Props {
  items: ResourceListItem[];
  showCategory?: boolean;
  showAuthor?: boolean;
  class?: string;
}

const { items, showCategory = false, showAuthor = true, class: className } = Astro.props;
---

<Card class:list={["overflow-hidden", className]}>
  <div class="divide-y divide-black/[0.05]">
    {items.map((item) => (
      <ResourceRow item={item} showCategory={showCategory} showAuthor={showAuthor} />
    ))}
  </div>
  <p data-empty-state hidden class="px-4 py-10 text-center text-sm text-neutral-500">
    No resources match your search.
  </p>
</Card>
